<% if game.trick_takinged_by?(current_user) %>
  <%= link_to "トリックテイキング", game_trick_takings_path(game), method: :delete, remote: true, class: "tag_button mechanics_color" %>
  <div class="tag_count"><%= @game.trick_takings.count %></div>
<% else %>
  <%= link_to "トリックテイキング", game_trick_takings_path(game), method: :post, remote: true, class: "tag_button yet_color" %>
  <div class="tag_count"><%= @game.trick_takings.count %></div>
<% end %>