<% if game.expanded_reproductioned_by?(current_user) %>
  <%= link_to "拡大再生産", game_expanded_reproductions_path(game), method: :delete, remote: true, class: "tag_button mechanics_color" %>
  <div class="tag_count"><%= @game.expanded_reproductions.count %></div>
<% else %>
  <%= link_to "拡大再生産", game_expanded_reproductions_path(game), method: :post, remote: true, class: "tag_button yet_color" %>
  <div class="tag_count"><%= @game.expanded_reproductions.count %></div>
<% end %>