<% if game.draftinged_by?(current_user) %>
  <%= link_to "競り", game_draftings_path(game), method: :delete, remote: true, class: "tag_button mechanics_color" %>
  <div class="tag_count"><%= @game.draftings.count %></div>
<% else %>
  <%= link_to "競り", game_draftings_path(game), method: :post, remote: true, class: "tag_button yet_color" %>
  <div class="tag_count"><%= @game.draftings.count %></div>
<% end %>